DATABASE_URL=postgresql://user:password@host/dbname
SECRET_KEY=changeme

# Recordatorios semanales por email (opcional -- si no se configuran, /internal/reminders/run
# responde 503 en vez de fallar el arranque de la app).
RESEND_API_KEY=re_xxxxxxxx
REMINDER_FROM_EMAIL=onboarding@resend.dev
REMINDER_TASK_TOKEN=generar-con-secrets.token_urlsafe
